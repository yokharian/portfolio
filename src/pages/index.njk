---
layout: base.njk
title: Home
---

<section id="hero" class="mb-12" aria-labelledby="hero-title">
  <div class="mx-auto max-w-6xl py-10 sm:py-16">
    <div class="flex flex-col lg:flex-row items-center gap-8">
      <div class="flex-1">
        <h1 id="hero-title" class="text-4xl sm:text-5xl font-bold tracking-tight" data-anim="heading">
          Python & AWS Solutions Architect
        </h1>
        <p class="mt-4 text-lg text-slate-600" data-anim="subheading">
          I design, build, and scale data-intensive systems in the cloud.
        </p>
        <div class="mt-6" data-anim="cta">
          <a href="/#projects" class="inline-block rounded-md bg-sky-600 px-5 py-3 text-white font-medium hover:bg-sky-700 focus:outline-none focus:ring-2 focus:ring-sky-500 focus:ring-offset-2">View Projects</a>
        </div>
      </div>
      <div class="flex-1 w-full max-w-sm lg:max-w-md">
        <figure class="relative mx-auto">
          <img src="/assets/images/profile.jpg" alt="Professional profile photo of Sofia" width="640" height="640" class="w-full h-auto rounded-xl shadow-md object-cover aspect-square" data-anim="image">
        </figure>
      </div>
    </div>
  </div>
</section>

<h2 class="text-2xl font-semibold mb-6">Latest Posts</h2>

<ul class="space-y-3">
  {% for post in collections.posts | reverse %}
    <li class="p-4 bg-white rounded-lg shadow-sm border">
      <a href="{{ post.url }}" class="text-sky-700 hover:underline font-medium">
        {{ post.data.title or post.fileSlug | capitalize }}
      </a>
      {% if post.date %}
        <span class="ml-2 text-xs text-slate-500">({{ post.date | date("yyyy-LL-dd") }})</span>
      {% endif %}
    </li>
  {% endfor %}
</ul>
