{#
  Project Card partial
  Usage: {% include "partials/project-card.njk" with { item: project } %}
#}
{% set data = item.data or {} %}
<article class="relative rounded-xl border border-slate-200 bg-white p-4 shadow-sm transition hover:shadow-md focus-within:shadow-md hover:-translate-y-0.5 focus-within:-translate-y-0.5 duration-200" role="listitem" aria-labelledby="proj-{{ item.fileSlug }}">
  <a href="{{ item.url }}" class="absolute inset-0 rounded-xl focus:outline-none focus-visible:ring-2 focus-visible:ring-sky-500 focus-visible:ring-offset-2 focus-visible:ring-offset-white" aria-label="View project: {{ data.title }}">
    <span class="sr-only">{{ data.title }}</span>
  </a>
  {% if data.heroImage %}
    <div class="mb-3 overflow-hidden rounded-lg">
      {% image data.heroImage, (data.alt or data.title), "(min-width: 1024px) 33vw, (min-width: 768px) 50vw, 100vw", "h-40 w-full object-cover transition-transform duration-200 group-hover:scale-[1.02]" %}
    </div>
  {% endif %}
  <h3 id="proj-{{ item.fileSlug }}" class="mb-1 text-lg font-semibold text-slate-900">{{ data.title }}</h3>
  {% if data.description %}
    {% set desc = data.description or '' %}
    <p class="mb-3 text-sm text-slate-600">{{ desc | truncate(120, true, '…') }}</p>
  {% endif %}
  {% if data.tags and data.tags.length %}
    <ul class="flex flex-wrap gap-2" aria-label="Technologies used">
      {% for t in data.tags %}
        {% if loop.index <= 5 %}
          <li><span class="inline-block rounded bg-slate-100 px-2 py-1 text-xs font-medium text-slate-700">{{ t }}</span></li>
        {% endif %}
      {% endfor %}
    </ul>
  {% endif %}
</article>
