{# SEO Meta Tags Component #}
{# 
  Props:
  - title: Page title (optional, defaults to site title)
  - description: Page description (optional, defaults to site description)
  - image: Social sharing image (optional, defaults to site image)
  - canonicalURL: Canonical URL (optional, defaults to current page URL)
  - type: Page type (optional, defaults to 'website')
  - publishedTime: Article published time (optional, for articles)
  - modifiedTime: Article modified time (optional, for articles)
  - author: Article author (optional, defaults to site author)
#}

{# Set defaults #}
{% set pageTitle = title or site.title %}
{% set pageDescription = description or site.description %}
{% set pageImage = image or (site.url + '/assets/images/profile.svg') %}
{% set pageType = type or 'website' %}
{% set canonicalURL = canonicalURL or (site.url + page.url) %}
{% set pageAuthor = author or site.title %}

{# Generate title tag with proper formatting #}
<title>{{ pageTitle }}{% if pageTitle != site.title %} | {{ site.title }} - {{ site.description }}{% endif %}</title>

{# Basic meta tags #}
<meta name="description" content="{{ pageDescription }}">
<meta name="author" content="{{ pageAuthor }}">

{# Canonical URL #}
<link rel="canonical" href="{{ canonicalURL }}">

{# Open Graph tags #}
<meta property="og:title" content="{{ pageTitle }}">
<meta property="og:description" content="{{ pageDescription }}">
<meta property="og:image" content="{{ pageImage }}">
<meta property="og:url" content="{{ canonicalURL }}">
<meta property="og:type" content="{{ pageType }}">
<meta property="og:site_name" content="{{ site.title }}">
<meta property="og:locale" content="{{ page.lang or site.defaultLanguage }}">

{# Twitter Card tags #}
<meta name="twitter:card" content="summary_large_image">
<meta name="twitter:title" content="{{ pageTitle }}">
<meta name="twitter:description" content="{{ pageDescription }}">
<meta name="twitter:image" content="{{ pageImage }}">
{% if site.twitterHandle %}<meta name="twitter:site" content="@{{ site.twitterHandle }}">{% endif %}
{% if site.twitterHandle %}<meta name="twitter:creator" content="@{{ site.twitterHandle }}">{% endif %}

{# Article specific meta tags #}
{% if pageType == 'article' %}
  {% if publishedTime %}<meta property="article:published_time" content="{{ publishedTime }}">{% endif %}
  {% if modifiedTime %}<meta property="article:modified_time" content="{{ modifiedTime }}">{% endif %}
  {% if author %}<meta property="article:author" content="{{ author }}">{% endif %}
  {% if tags %}{% for tag in tags %}<meta property="article:tag" content="{{ tag }}">{% endfor %}{% endif %}
{% endif %}

{# Additional SEO meta tags #}
<meta name="robots" content="index, follow">
<meta name="googlebot" content="index, follow">
<meta name="theme-color" content="#0ea5e9">
<meta name="msapplication-TileColor" content="#0ea5e9">

{# Language alternates (already handled in base layout) #}
{# These are handled in the base layout for consistency #}
