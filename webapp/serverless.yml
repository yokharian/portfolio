service: yokharian-portfolio

variablesResolutionMode: 20210219
useDotenv: true

provider:
  name: aws
  memorySize: 512 # Overwrite the default memory size. Default is 1024
  runtime: python3.8
  stage: prod
  region: us-east-1
  apiGateway:
    shouldStartNameWithService: true
  iam:
    ${file(serverless_manifests/iam_permissions.yml)}
  lambdaHashingVersion: 20201221
  deploymentBucket:
    name: yokharian-deployment-bucket # Deployment bucket name. Default is generated by the framework
    blockPublicAccess: true # Prevents public access via ACLs or bucket policies. Default is false
    skipPolicySetup: false # Prevents creation of default bucket policy when framework creates the deployment bucket. Default is false
    maxPreviousDeploymentArtifacts: 3 # On every deployment the framework prunes the bucket to remove artifacts older than this limit. The default is 5

functions:
  app:
    memorySize: 256
    handler: wsgi_handler.handler
    layers:
      - Ref: PythonRequirementsLambdaLayer
    events:
      - http: ANY /
      - http: "ANY {proxy+}"

custom:
  wsgi:
    app: entrypoint_prod.server
    pythonBin: python3
    packRequirements: false
  pythonRequirements:
    layer: true
    # https://stackoverflow.com/q/66845303/
    dockerizePip: true
    slim: true
    strip: false

  customDomain:
    domainName: yokharian.dev
    stage: prod
    certificateName: 'yokharian.dev'
    createRoute53Record: true
    createRoute53IPv6Record: true
    endpointType: 'edge'
    securityPolicy: tls_1_2
    apiType: rest
    autoDomain: false

plugins:
  - serverless-wsgi
  - serverless-python-requirements
  - serverless-deployment-bucket
  - serverless-plugin-optimize
  - serverless-domain-manager

package:
  exclude:
    - node_modules/**
    - package.json
    - package-lock.json
    - requirements.txt